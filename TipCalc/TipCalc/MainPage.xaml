<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TipCalc"
             x:Class="TipCalc.MainPage">

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Enter In Total Bill Before Tip"/>
            <Entry x:Name="billAmount" Text="20.00" />  <!-- Bind to TipCalc -->
            <Label Text="Enter In Total Tip Amount"/>
            <Entry x:Name="tipAmount" Text="5.00"/> <!-- Bind to TipCalc-->
            <Button x:Name="translateButon" Text="Click Button To Fire Command!" Command="{Binding TipCommand}"/>  <!-- Run TipCalc-->
            <Label Text="{Binding GetPercentage, Mode=OneWay}"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
